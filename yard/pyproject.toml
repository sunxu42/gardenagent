[project]
name = "yard-manager-agent"
version = "0.1.0"
description = "用于庭院管理的智能体，支持基础对话、机械臂使用和庭院维护。"
requires-python = ">=3.11"
dependencies = [
    "deepagents>=0.3.5",
    "pillow>=10.0.0",
    "pyyaml>=6.0.0",
    "rich>=13.0.0",·
]

[dependency-groups]
dev = []
